{
    # What kinds of plots should be made?
    "plottype": "param_corner", # possible types: all, heatmap, trace, corner, residual
    "upper_rho_prior" : 10, # what the upper limit of the density prior is
    "uniform_densities" : true, # if the densities (and masses) are uniform (typically no, but is an option for visualization purposes)
    "model_id" : 0, # which kind of parametric model is being run

    "nburnin" : 10, # number of burnin steps the emcee run took (should match whatever runprops has for this value)

    # Input/output data files
    "init_filename":"../runs/init_guess.csv",
    "priors_filename": "../runs/init_priors.csv",
    "input_data_filename": "../data/thinned/all_thin.csv",  # PhoDyMM thinned posteriors (with calculated values)
    "results_folder":"../results",                          # where the results go
    "backend_path": "../results/grid/backend_",                  # the emcee backend location
    "param_result_filename": "param_model_0.h5",
    
    # Additional options
    "verbose" : true,             # Enables print statements within the code
    "make_gifs" : true,           # creates gifs from the plots
    "is_plot_ids" : true,         # Whether the plot should include voxel ids
    "fps" : 0.3,                  # frames per second for gifs    
    "heatmap_plot_type" : "all",  # type of heatmap plot to create (should probably be set to all unless debugging)
    "residual_plot_type" : "all"  # type of residual plots to create (again, should probably be set to all)
}